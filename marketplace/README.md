# Marketplace (Local Index)

This folder is a local-first “marketplace”:
- `index.json` is the catalog
- `packages/` stores `.kaizen` files

Commands:
```bash
python tools/kaizen_market.py list
python tools/kaizen_market.py install-index --name skill-marketplace --root .
```

